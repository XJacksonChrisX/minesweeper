# 踩地雷遊戲

## 系級班級
資訊工程系 - 大學部

## 成員資訊
| 角色 | 姓名 | 學號 | 負責內容 |
|------|------|------|--------|
| 組長 | （待填寫） | （待填寫） | 專案規劃與整合 |
| 組員 | （待填寫） | （待填寫） | 程式設計與測試 |

## 小專題題目
**踩地雷遊戲 (Minesweeper Game)**

## 程式介紹

### **專案概述**
這是一個使用 C++ 開發的經典踩地雷遊戲。遊戲提供三個難度等級，玩家需要通過邏輯推理和運氣來揭露所有非地雷的格子，同時避免踩到地雷。

### **遊戲規則**
1. 玩家選擇難度等級後，遊戲會生成一個隨機布置地雷的棋盤
2. 玩家可以通過輸入座標來揭露格子
3. 揭露的格子會顯示：
   - **💣**：地雷（遊戲失敗）
   - **·**：空格（周圍沒有地雷）
   - **1-8**：相鄰地雷個數
4. 玩家也可以標記懷疑的地雷位置（**🚩**）
5. 揭露所有非地雷格子即獲勝

### **難度等級**
| 難度 | 棋盤大小 | 地雷數量 |
|------|---------|--------|
| 簡單 | 8×8 | 10 |
| 中等 | 12×12 | 30 |
| 困難 | 16×16 | 40 |

### **系統架構**

#### **類別設計 (UML 類圖)**

```
┌─────────────────────────────────────┐
│             Cell                    │
├─────────────────────────────────────┤
│ - isMine: bool                      │
│ - isRevealed: bool                  │
│ - isMarked: bool                    │
│ - adjacentMines: int                │
├─────────────────────────────────────┤
│ + getMine()                         │
│ + getRevealed()                     │
│ + getMarked()                       │
│ + getAdjacentMines()                │
│ + setMine(bool)                     │
│ + setRevealed(bool)                 │
│ + setMarked(bool)                   │
│ + setAdjacentMines(int)             │
└─────────────────────────────────────┘
          ▲
          │ composition (rows*cols)
          │
┌─────────────────────────────────────┐
│             Board                   │
├─────────────────────────────────────┤
│ - board: Cell**                     │
│ - rows: int                         │
│ - cols: int                         │
│ - totalMines: int                   │
│ - revealedCells: int                │
├─────────────────────────────────────┤
│ + initialize()                      │
│ + getCell(int, int)                 │
│ + revealCell(int, int)              │
│ + markCell(int, int)                │
│ + isValid(int, int)                 │
│ + calculateAdjacentMines()          │
└─────────────────────────────────────┘
          ▲
          │ composition
          │
┌─────────────────────────────────────┐
│             Game                    │
├─────────────────────────────────────┤
│ - board: Board*                     │
│ - state: GameState                  │
│ - difficulty: int                   │
├─────────────────────────────────────┤
│ + start()                           │
│ + play()                            │
│ + displayBoard()                    │
│ + checkGameState()                  │
│ + revealCell(int, int)              │
│ + markCell(int, int)                │
│ + floodFill(int, int)               │
└─────────────────────────────────────┘
          ▲
          │ uses
          │
┌─────────────────────────────────────┐
│            main()                   │
├─────────────────────────────────────┤
│ - 接收使用者難度選擇                │
│ - 建立 Game 物件                    │
│ - 開始遊戲                          │
└─────────────────────────────────────┘
```

### **主要功能**

#### **1. Cell 類別**
表示棋盤上的單一格子，記錄：
- 是否為地雷
- 是否已被揭露
- 是否被標記
- 相鄰地雷數量

#### **2. Board 類別**
管理整個遊戲棋盤：
- 動態分配二維陣列存儲棋盤
- 隨機放置地雷
- 計算每個格子的相鄰地雷數
- 驗證座標有效性

#### **3. Game 類別**
核心遊戲邏輯：
- 根據難度初始化棋盤
- 顯示棋盤和操作提示
- 處理玩家輸入命令
- 檢查遊戲勝敗條件
- 實現泛洪填充算法（自動揭露連通的空格）

#### **4. main 函數**
遊戲入口點：
- 顯示難度選擇菜單
- 建立遊戲物件
- 啟動遊戲主迴圈

### **技術實現**

#### **使用的 C++ 特性**
- **動態記憶體管理**：使用指標管理棋盤二維陣列
- **類別與物件導向設計**：各功能模組化
- **標準模板庫 (STL)**：使用 `queue` 實現泛洪填充
- **結構化綁定**（C++17）：簡化資料結構操作

#### **核心算法**

**泛洪填充 (Flood Fill)**
當玩家揭露一個空格（周圍沒有地雷）時，自動揭露所有相鄰的格子，直到遇到有數字的邊界。這提高了遊戲體驗。

**相鄰地雷計算**
針對每個非地雷格子，檢查其 8 個相鄰方向，計算地雷個數。

### **檔案結構**

```
game/
├── CMakeLists.txt              # CMake 建置配置
├── README.md                   # 本文件
├── include/                    # 頭文件目錄
│   ├── cell.h                  # Cell 類別定義
│   ├── board.h                 # Board 類別定義
│   └── game.h                  # Game 類別定義
└── src/                        # 源代碼目錄
    ├── main.cpp                # 程式入口
    ├── cell.cpp                # Cell 類別實現
    ├── board.cpp               # Board 類別實現
    └── game.cpp                # Game 類別實現
```

## 編譯與執行

### **系統要求**
- C++17 或更高版本
- CMake 3.10 或更高版本
- Linux/macOS/Windows (支援 C++ 編譯器)

### **編譯步驟**

#### **在 Linux/macOS 上：**

```bash
# 進入專案目錄
cd game

# 建立並進入 build 目錄
mkdir -p build
cd build

# 使用 CMake 生成 Makefile
cmake ..

# 編譯專案
make

# 執行遊戲
./minesweeper
```

#### **在 Windows 上：**

```bash
# 進入專案目錄
cd game

# 建立並進入 build 目錄
mkdir build
cd build

# 使用 CMake 生成 Visual Studio 專案
cmake ..

# 使用 Visual Studio 開啟並編譯
# 或使用 cmake --build . 命令

# 執行遊戲
minesweeper.exe
```

### **執行遊戲**

```
輸出範例：
========== 踩地雷遊戲 ==========
請選擇難度:
1. 簡單   (8x8 棋盤, 10個地雷)
2. 中等   (12x12 棋盤, 30個地雷)
3. 困難   (16x16 棋盤, 40個地雷)
請輸入難度 (1-3): 1

========== 歡迎來到踩地雷遊戲 ==========
難度: 簡單 (8x8, 10個地雷)
========================================

  0 1 2 3 4 5 6 7 
0 □ □ □ □ □ □ □ □ 
1 □ □ □ □ □ □ □ □ 
2 □ □ □ □ □ □ □ □ 
3 □ □ □ □ □ □ □ □ 
4 □ □ □ □ □ □ □ □ 
5 □ □ □ □ □ □ □ □ 
6 □ □ □ □ □ □ □ □ 
7 □ □ □ □ □ □ □ □ 

操作命令:
  r row col  - 揭露 (row, col) 的格子
  m row col  - 標記 (row, col) 為地雷
  q          - 退出遊戲

輸入命令: r 3 3
```

### **遊戲操作範例**

| 命令 | 功能 | 範例 |
|------|------|------|
| `r row col` | 揭露指定座標的格子 | `r 3 3` |
| `m row col` | 標記/取消標記地雷 | `m 2 5` |
| `q` | 退出遊戲 | `q` |

## 程式設計概念應用

### **物件導向設計 (OOP)**
- **封裝 (Encapsulation)**：每個類別管理自己的數據和操作
- **繼承 (Inheritance)**：未來可擴展不同類型的遊戲模式
- **多型 (Polymorphism)**：可定義不同難度的初始化邏輯

### **資料結構**
- **二維陣列**：存儲棋盤狀態
- **佇列 (Queue)**：實現泛洪填充算法

### **演算法**
- **泛洪填充算法**：廣度優先搜索 (BFS)
- **隨機數生成**：地雷位置的隨機化

## 可能的擴展功能

1. **計時器**：記錄完成遊戲所需時間
2. **排行榜**：存儲最佳成績
3. **標記計數**：顯示已標記地雷的數量
4. **自訂棋盤大小**：允許玩家自訂難度參數
5. **圖形介面**：使用 Qt 或 SDL 開發 GUI 版本
6. **遊戲統計**：記錄勝率、平均用時等統計數據

## 學習成果

透過本專題，學習者應能：
- ✅ 掌握 C++ 基本物件導向程式設計
- ✅ 理解類別的設計與使用
- ✅ 實現基本的遊戲邏輯
- ✅ 使用動態記憶體管理
- ✅ 應用常見演算法（泛洪填充）
- ✅ 建立和執行 CMake 專案

## 參考資源

- [C++ 官方文檔](https://en.cppreference.com/)
- [CMake 官方文檔](https://cmake.org/documentation/)
- [Minesweeper 維基百科](https://zh.wikipedia.org/wiki/踩地雷)

---

**最後更新日期**：2026年4月22日  
**版本**：1.0